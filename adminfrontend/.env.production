# Production environment variables for Admin
# These are used during build on Render

# Backend API base URL (must end with /api)
VITE_API_BASE=https://ecommerce-backend.onrender.com/api

# Node environment
NODE_ENV=production
